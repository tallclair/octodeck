# Known Bugs

- The ack button on the pull request card has p-0.5 property on hover but p-0 otherwise. This change in height causes the activity indicator to shift down slightly when the button appears.
- Controls (ack, star, notes, etc) should be disabled when the dashboard is offline
