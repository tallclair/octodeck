export const DEFAULT_PORT = 38274;
export const DEFAULT_HOST = '127.0.0.1';
export const DEFAULT_BASE_URL = `http://${DEFAULT_HOST}:${DEFAULT_PORT}`;
export const DEFAULT_API_BASE_URL = `${DEFAULT_BASE_URL}/api/v1`;

export const DEFAULT_POLLING_INTERVAL_MIN = 1;
export const DEFAULT_AUTO_ACK_OWN_ACTIVITY = true;

